apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana-dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      labels:
        app: grafana
    spec:
      containers:
      - name: grafana
        image: owlbandocker/dashboard:latest
        ports:
        - containerPort: 3000
